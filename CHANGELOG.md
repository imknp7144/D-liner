# Changelog

D-liner の変更履歴です。開発中の詳細なセッションログ・内部監査記録は非公開
リポジトリで管理しており、本ファイルにはユーザーに影響する変更のみを要約して
掲載しています。

## [0.8] - 2026-07-25

### Added
- タグパネルの検索/コピーモード切り替えに `S` キーショートカットを追加
- 個別設定でのタグ付けやり直し機能（画像・フォルダ単位で一時的にモデル・
  閾値を変更して再タグ付け。設定は保存されない一回限りの操作）
- LoRA用エクスポート機能（選択画像を新規フォルダへ「画像コピー＋同名.txt
  キャプション」としてエクスポート）
- ファイル名の自然順ソート対応
- SDIウィンドウのサイズ記憶機能
- 一般タグ閾値を極端に低く設定した場合の警告表示

### Fixed
- コピーモードを維持する設定でSDIウィンドウを閉じ、別画像を開いた際に背景色
  だけ検索モードの色へ戻ってしまう不具合を修正
- 一般タグ閾値の低下警告が、スピナー操作で閾値を下げた場合に正しく表示され
  ないことがある不具合を修正（浮動小数点誤差が原因）
- 「選択中の画像を別設定でタグ付けし直す」ダイアログに低閾値警告が実装され
  ていなかった点を修正

## [0.7] - 2026-07-19

### Added
- タグパネルに「コピーモード」を追加。選択したタグをクリップボードへコピー、
  または組み合わせでの再検索が可能に
- 「全タグをコピー」ボタンを追加
- 「コピーモード終了時の挙動」設定を追加（検索モードに戻す/維持する）
- サムネイル一覧の右クリックメニューに「似たタグの画像を探す」を追加

### Fixed
- 複数選択中の右クリックで「名前の変更」が実行でき、1件だけ密かにリネーム
  されてしまう不具合を修正
- ウィンドウ幅を狭めた際、メニューバー右端のアイコンが通常メニュー項目を
  圧迫していた不具合を修正

## [0.65] - 2026-07-18

### Added
- 手動タグ機能（タグパネルの右クリックメニュー、`T` キーショートカット、
  オートコンプリート付き）
- AI自動タグ付けのロック機能（画像単位で自動タグ付け対象から除外）
- 複数画像への一括タグ追加/削除機能

### Changed
- タグの削除方法を「×」クリック方式に変更

### Fixed
- 検出タグ数が多い画像でタグの一部が表示されない不具合を修正
- 一括タグ操作実行時にアプリが異常終了することがある不具合を修正し、
  想定外のエラー発生時はダイアログ表示に留める仕組みを追加

## [0.6] - 2026-07-12

### Added
- キーボードショートカットを追加（`Ctrl+C`/`Ctrl+X`/`Ctrl+V` によるファイル
  操作、`F2` リネーム、`F8` 新規フォルダ作成、`Ctrl+F` 検索欄フォーカス、
  `Escape` 検索クリア）
- ファイル/フォルダ削除をゴミ箱経由（`send2trash`）に変更

### Fixed
- フォルダ削除時の `[WinError 5]` エラーを修正
- ゴミ箱への移動時のパス区切り文字不整合によるエラーを修正
- `Delete` キーによる削除が反応しない不具合を修正
- `F8` での新規フォルダ作成時に表示中フォルダが意図せず切り替わる不具合を修正
- 複数選択ファイルのドラッグ&ドロップで1件しかコピー/移動されない不具合を修正

## [0.5] - 2026-07-11 (プレリリース版)

### Fixed
- モデル自動ダウンロード時のハング・クラッシュ問題を解決
- DirectML誤検出バグを修正

### Added
- 「モデルフォルダ(手動指定)」機能を新設
- NPU非搭載機のデバイス選択肢からNPUを除外
