# D-liner ユーザーマニュアル（プレリリース版 v0.8）

作成日: 2026年7月（v0.8: 2026年7月25日更新）

本書は、D-liner を初めて導入・利用するユーザー向けの説明資料です。特にプレリリース版として、動作前提となる環境要件（Python / Visual C++ 再頒布可能パッケージなど）と、実際の使用感に関わる注意点（バックグラウンド処理・メモリ使用量など）を優先して記載しています。

D-liner は個人利用を前提としたローカル完結型のアプリケーションです。外部サーバーへ画像や生成物をアップロードすることはありません（初回のみ、タグ付けモデル取得のために Hugging Face へアクセスします）。

---

## 目次

1. D-liner とは
2. 動作環境・前提条件
3. インストール手順
4. 初回起動時にご確認いただくこと
5. タグ付け設定
6. 画面の基本操作
7. バックグラウンド処理について（ご利用前に必ずお読みください）
8. メモリ使用に関する注意事項
9. 既知の制約事項
10. トラブルシューティング
11. お問い合わせについて
12. 変更履歴

---

## 1. D-liner とは

D-liner は、AI画像生成物（イラスト・生成画像等）を対象とした画像管理・タグ付けアプリケーションです。フォルダ単位での画像閲覧に加え、WD14 / Camie / JoyTag といったタガーモデルによる自動タグ付けをローカル環境（NPU / GPU / CPU）で実行し、タグによる検索・整理を行えます。

本書の対象読者は、D-liner を初めて自分の環境にセットアップするユーザーです。すでに旧バージョン（v0.5）をご利用の場合も、2章「動作環境・前提条件」と7章「バックグラウンド処理について」は変更点を含むため、目を通すことを推奨します。

---

## 2. 動作環境・前提条件

D-liner の実行には、以下の環境が前提となります。インストール作業（3章）を始める前にご確認ください。

### OS

Windows 10 / 11（64bit）。VC++ 再頒布可能パッケージの自動検出や NPU 検出など、環境確認の一部処理は Windows 専用です。

### Python

- **3.10 以上が必須です。**
- システムの Python（venv を作成できるもの）が別途必要です。事前に python.org 等からインストールし、コマンドラインから `python` が実行できる状態にしてください。
- D-liner 自体は専用の仮想環境（venv）内で動作するため、システム側の Python に他の用途で入れているライブラリと衝突する心配はありません。

### Visual C++ 再頒布可能パッケージ

- **Microsoft Visual C++ 2015-2022 Redistributable (x64) が必要です。**
- タグ付け機能が利用する onnxruntime 系パッケージ（NPU/GPU/CPU いずれの構成でも）が `VCRUNTIME140.dll` 等に依存しているためです。
- 未導入の場合、セットアップ時に警告が表示されますが処理自体は続行されます。ただし後述の「onnxruntime のインポートに失敗しました」エラーの主原因になるため、事前導入を強く推奨します。
- 配布元: https://aka.ms/vs/17/release/vc_redist.x64.exe

### ディスク空き容量

- 実行用仮想環境（PyQt6 / numpy / onnxruntime 等）だけで数百MB程度。
- タグ付けモデルを自動ダウンロードする場合、1モデルあたり数百MB〜1GB程度の追加容量が必要です（wd14 / camie / joytag のいずれを使うかで変動します）。
- 画像管理DB（linar.db）はサムネイルキャッシュや管理情報を保持するため、扱う画像枚数に応じて増加します。

### ネットワーク接続

- タグ付けモデル（wd14 / camie / joytag のいずれか）が手元に無い場合、初回タグ付け実行時に Hugging Face から自動ダウンロードを試みます。
- 社内プロキシ・ファイアウォール等でアクセスがブロックされている環境では自動ダウンロードが失敗（またはタイムアウト）します。この場合は4-3節「モデルフォルダ(手動指定)」の手順で回避できます。
- 通常利用時（タグ付け・閲覧・検索）はネットワーク接続を必要としません。

### メモリ (RAM)

目安として 8GB 以上を推奨します。ただし7章「バックグラウンド処理について」・8章「メモリ使用に関する注意」に記載の通り、扱う画像の枚数・解像度によって実際の消費量は変動します。

### NPU / GPU

- 必須ではありません。無い場合は CPU でタグ付けが動作します（速度は低下します）。
- Intel NPU（Core Ultra シリーズ等）があれば OpenVINO 経由で、NVIDIA/AMD/Intel の GPU があれば DirectML 経由でそれぞれ高速化されます。
- 重要な制約があります。詳細は9章「既知の制約事項」を必ずご確認ください。

> **特に見落としやすい点**
> - 「Python が無くても動く配布」ではありません。D-liner は同梱の `setup_runtime_env.py` がシステム Python を使って専用venvを作る方式のため、Python 3.10 以上の事前インストールが必須です。
> - Visual C++ 再頒布可能パッケージが未導入だと、一見セットアップは完了したように見えても実際にタグ付け機能を使った瞬間にエラーになることがあります。事前導入を推奨します。

---

## 3. インストール手順

### 3-1. セットアップスクリプトの実行

配布フォルダ内の `setup_runtime_env.bat` をダブルクリックしてください（内部で `setup_runtime_env.py` が実行されます）。このスクリプトは以下を自動で行います。

- Python バージョンの確認（3.10未満の場合はここで中止します）
- Visual C++ 再頒布可能パッケージの導入確認（未導入の場合は警告のみ表示し、続行します）
- Intel NPU の有無を自動検出し、実行用ランタイム系統（npu / directml / cpu）を決定
- 専用の仮想環境（`d_liner_runtime_env` フォルダ）の作成とパッケージのインストール
- 実際にセッションを作成して NPU/GPU が本当に使えるかの動作確認（probe）
- 起動用ランチャー（`launch_d_liner.bat` / `launch_d_liner_debug.bat`）の生成

NPU非搭載機・NVIDIA/AMD GPU搭載機など、自動検出の結果を上書きしたい場合はコマンドラインから以下のように実行してください。

```
setup_runtime_env.bat --runtime directml
```

（`--runtime` の指定値: `auto`（既定・自動検出） / `npu` / `directml` / `cpu`）

### 3-2. アプリの起動

- `launch_d_liner.bat` … 通常起動（コンソール非表示）。普段はこちらを使用してください。
- `launch_d_liner_debug.bat` … デバッグ起動（コンソール表示）。エラーで起動しない場合や不具合調査時に使用します。

---

## 4. 初回起動時にご確認いただくこと

### 4-1. フォルダの登録

D-liner はフォルダ単位で画像を管理します。初回起動時は画像フォルダをフォルダツリーから指定し、スキャンを行ってください。監視モードとしてフォルダを登録すると、起動時に自動で差分チェックが行われます。

### 4-2. タグ付けモデルの準備

タグ付け設定（詳細は5章）で使用するモデル（wd14 / camie / joytag のいずれか）を選択します。モデルファイルが手元に無い場合、タグ付けを実行したタイミングで Hugging Face から自動ダウンロードが試みられます。

> **初回タグ付け時の注意**
> - 自動ダウンロードにはネットワーク接続が必要です。回線速度によっては数分〜十数分かかることがあります（モデルサイズは数百MB〜1GB程度）。
> - ダウンロードがタイムアウトした場合、アプリ本体は落ちずに「今回は失敗」として処理が継続されます（同一モデルの再試行は、アプリ再起動または再接続まで自動的に抑制されます）。
> - 社内ネットワーク等でHuggingFaceへのアクセス自体がブロックされている場合は、下記4-3の手動配置をご利用ください。

### 4-3. 自動ダウンロードが使えない環境向け: モデルフォルダの手動指定

タグ付け設定ダイアログの「モデルフォルダ(手動指定)」欄に、あらかじめ手動でダウンロードした `model.onnx` とタグ定義ファイルを置いたフォルダを指定することで、自動ダウンロードに頼らずタグ付け機能を利用できます。以下いずれの指定方法にも対応しています。

- 指定フォルダの直下に `model.onnx` がある場合 … そのフォルダを直接指定
- 複数モデルを1つの親フォルダにまとめて管理している場合（例: `E:/models`）… 親フォルダを指定すると、`E:/models/{モデルID}/model.onnx` を自動探索します

---

## 5. タグ付け設定

メニューから「タグ付け設定」を開くと、以下の項目を設定できます。

| 項目 | 内容 |
|---|---|
| モデル | wd14 / camie / joytag から選択します。wd14 は版権キャラクター認識にやや弱い（学習データが古い）傾向があるため、より新しいタガーへの選択肢として camie / joytag を用意しています。 |
| デバイス | NPU / GPU / CPU から選択します（NPU非搭載機では NPU 自体が選択肢に表示されません）。 |
| 使用GPU | GPU が複数存在する環境でのみ表示され、使用するGPUを選べます。 |
| 各種閾値 | 一般タグ・キャラクタータグ・版権タグそれぞれの閾値（0.00〜1.00）を調整できます。値を上げるほど厳選されたタグのみが付与されます。 |
| モデルフォルダ(手動指定) | 4-3節を参照。空欄の場合は自動探索・自動ダウンロードが行われます。 |

モデルまたは閾値を変更すると、既存にタグ付け済みの画像との一貫性が崩れる可能性がある旨の確認が表示されます。必要に応じてタグキャッシュをクリアし、新しい設定で再タグ付けすることができます（強制ではありません）。

> **一般タグ閾値を極端に低く設定した場合の注意（v0.8で追加）**
> 一般タグの閾値を0.15以下に設定してOKを押すと、警告ダイアログが表示されます。閾値を極端に低くすると1枚の画像に数百件規模のタグが付与されることがあり、その画像をSDIウィンドウで開いた際にタグパネルの表示処理が一時的にフリーズすることが確認されているためです（強制終了はせず、しばらく待てば解放されます）。警告が出ても設定の保存やタグ付け処理自体はブロックされません。同様の警告は、後述6-8節「個別設定でのタグ付けやり直し」ダイアログでも一般タグ閾値が0.15以下の場合に表示されます。

---

## 6. 画面の基本操作

- **フォルダツリー**: 左側にフォルダ階層を表示します。クイックアクセス登録したフォルダはワンクリックでアクセスできます。
- **サムネイル一覧**: 選択フォルダ内の画像をサムネイル表示します。ドラッグ&ドロップでフォルダツリーへコピー（左ドラッグ）・移動メニュー呼び出し（右ドラッグ）ができます。
- **プレビュー（SDIウィンドウ）**: サムネイルをダブルクリックすると原寸に近い表示で画像を確認できます。ウィンドウは中クリックで閉じられます。
- **ステータスバー**: 右下のアイコンで、サムネイル生成・タグ付けの進行状況を常時表示します。🟢=処理中 / 🌙=待機（アイドル処理予約中） / ⚪=完了、の3状態です。

### 6-1. タグパネル（SDIウィンドウ下部）

SDIウィンドウで画像を開くと、下部にその画像のタグが一覧表示されます。

- **タグ本体をクリック**: メインウィンドウの検索欄にそのタグを追加し、絞り込み検索を行います。
- **タグ右側の「×」をクリック**: そのタグを削除します。AI由来タグ・手動追加タグのどちらも削除できます（AIの誤タグ修正が主な用途です）。
  - **手動追加タグ（後述）を削除する場合のみ、確認ダイアログが表示されます。** AI由来タグと異なり、手動タグは再タグ付けをしても復元されないためです。
- **タグが3行を超える場合**: 3行目の末尾に「+N件」という表示が出ます。クリックすると残り全件が展開表示されます（画像を切り替えると再び3行表示に戻ります）。
- **パネルの空白部分を右クリック**: 「タグを追加...」「既存タグ一覧から選択...」のメニューが開きます（詳細は6-2節）。

### 6-2. 手動タグの追加

AIタガーが知らない要素（LoRA学習用のトリガーワードなど）を追加したい場合や、AIが付けなかったタグを補いたい場合に使います。

- タグパネルを右クリック→「タグを追加...」で入力ダイアログが開きます。`Enter`で確定・`Esc`でキャンセルできます。入力中は既存タグからのオートコンプリート候補が表示されます。
- データベースにまだ存在しない新しいタグ名を入力した場合のみ、「新しいタグを追加しますか？」という確認が表示されます（誤字防止のためです）。既存タグを選んだ場合は確認なしで追加されます。
- 「既存タグ一覧から選択...」を選ぶと、DB全体でよく使われているタグ（この画像に未付与のもの）から選んで追加できます。手動で追加したタグ（LoRA学習用のトリガーワード等）も候補に含まれるため、以前使ったトリガーワードを別の画像にも使い回したい場合にご利用いただけます。検索欄で絞り込むこともでき、検索してもタグが見つからない場合は一覧末尾の「＋新規タグとして追加」から、その場で新規タグとして追加することもできます（`Enter`キーでも同じ操作ができます）。
  - **既にこの画像に付与済みのタグは候補に表示されません。** そのため、たとえば直前の再タグ付けで新たに検出されたタグをこの画像自身に対して探しても、一覧には出てきません（既に付いているため対象外という仕様上の動作です）。
  - **画像枚数・タグ数が多い環境では、検索時に動作がやや重くなることがあります。** DB全体を対象に検索する仕様上の制約のため、大量の画像を管理している場合はご了承ください。
- 手動で追加したタグは専用の色（金色）で先頭に表示され、`manual`という区分になります。
- **手動タグはタグ付けのやり直しで消えません。** 「タグキャッシュをクリア」やAIによる再タグ付けを行っても、手動追加したタグはそのまま保持されます。
- SDIウィンドウで `T` キーを押すと、「タグを追加」ダイアログを直接開けます（6-7節のベアキーと同系統のショートカットです）。

### 6-3. AI自動タグ付けのロック（画像単位）

画像右上の🔓/🔒アイコンをクリックすると、その画像を今後のバックグラウンド自動タグ付け対象から除外（ロック）できます。誤タグを手動で調整し、以後AIに触らせたくない画像がある場合にご利用ください。

- 🔓（アンロック中）をクリックするとロックされ、アイコンが🔒に変わります。もう一度クリックすると解除されます。
- **ロック/解除は、このアイコンをクリックした場合にのみ変化します。** タグの追加・削除、再タグ付けなど他の操作では自動的に変化しません。
- ロックした画像は、「タグキャッシュをクリア」を実行してもロック状態は解除されません（明示的にアイコンをクリックしない限り、ロックされたまま保持されます）。
- ロック中でも、選択画像に対する明示的な「再タグ付け」操作（メニューから個別に実行するもの）は通常通り行えます。ロックが影響するのはあくまでバックグラウンドの自動タグ付けのみです。

### 6-4. 複数画像への一括タグ追加/削除

LoRA学習用のトリガーワードを大量の画像へまとめて仕込む、といった用途を想定した機能です。

- メインウィンドウの「タグ」メニュー、またはサムネイル一覧の右クリックメニューから「選択中の画像にタグを一括追加/削除...」を実行します。サムネイル一覧にフォーカスがある状態で `T` キーを押しても同じダイアログが開きます。
- **対象範囲**: サムネイル一覧で1件以上選択していればその画像群、何も選択していなければ現在の絞り込み結果（表示中の画像）全体が対象になります。実行前に対象件数が確認ダイアログに表示されます。
- タグ名は複数指定可能です（スペース区切り）。追加されたタグは`manual`区分として扱われます。
- 対象件数が多い場合でも進捗ダイアログが表示され、途中でキャンセルもできます。

### 6-5. タグパネルのコピーモード

ComfyUI等へタグを貼り付けたい場合や、複数タグを組み合わせて再検索したい場合に使う機能です。SDIウィンドウのタグパネルは「検索モード」（既定・従来通り）と「コピーモード」の2状態を切り替えられます。

- メニューバー右端の🔍/📋アイコンをクリックすると、検索モード⇄コピーモードを切り替えられます。ホバーすると現在のモードと次にクリックした時の動作がツールチップで確認できます。
- **検索モード**（既定）: 従来通り、タグ本体クリックで検索欄に追記します。
- **コピーモード**: パネルの背景色が暖色系（焦茶〜赤系）に変わり、一目でモードが分かるようになります。このモード中は、タグ本体クリックのたびにそのタグが選択（トグル）され、選択中のタグは青くハイライトされます。誤操作防止のため、コピーモード中はタグの×削除ボタンが無効化されます。
- 1件以上選択すると、パネル下部に「コピー」「この組み合わせで検索」という小さなボタンが現れます。
  - **コピー**: 選択したタグを`rating`/`meta`カテゴリを除いたカンマ区切り文字列としてクリップボードへコピーします。
  - **この組み合わせで検索**: メインウィンドウの検索欄を選択タグのみで置き換え、AND検索を実行します（元々入力していた内容は上書きされます）。
- 別の画像に切り替えると、選択状態は自動的にリセットされます。
- メニューバー右端の🏷アイコン（「全タグをコピー」）は、検索/コピーどちらのモードでも常に使えます。3行に折りたたまれて非表示になっているタグも含め、表示中の全タグをコピーします。
- モードの状態はアプリ再起動後も維持されます。SDIウィンドウを閉じた際にコピーモードをそのまま維持するか、検索モードに自動的に戻すかは、「オプション」→「コピーモード終了時の挙動」で選べます（既定は「閉じたら検索モードに戻す」）。

> **検索/コピーモードの切り替えはSキーでも可能です（v0.8で反映漏れを修正。機能自体はセッション26相当で実装済みでした）**
> メニューバー右端のアイコンクリックに加え、SDIウィンドウの「編集」メニュー→「検索/コピーモード切替」（ショートカット: `S`）でも切り替えられます。他のベアキー同様、SDIウィンドウがアクティブな状態でのみ有効です。

### 6-6. 似たタグの画像を探す

サムネイル一覧で画像を右クリックすると「似たタグの画像を探す」という項目があります。実行すると、その画像のキャラクタータグ（最大2件）・一般タグ（最大3件）を自動抽出して検索欄にセットし、似た構図・要素を持つ他の画像を検索します。`1girl`/`solo`等、ほとんどの画像に付いている汎用タグは自動的に除外されるため、実質的に絞り込みにならない状態にはなりません。

- フォルダ行、または未登録フォルダ内の画像を右クリックした場合は、この項目自体が表示されません。
- 複数の画像を選択した状態で右クリックした場合も、この項目は表示されません（単一の画像を参照する機能のため）。

### 6-7. キーボードショートカット

D-linerのショートカットキーには、由来の異なる2系統があります。

- **修飾キー方式（Windows標準）**: `Ctrl+C` のように、他の一般的なアプリと同じ感覚で使えるものです。フォーカスがどのペイン（サムネイル一覧・フォルダツリー・検索欄など）にあっても効きます。
- **ベアキー方式（linar由来）**: `C` 単体のように、修飾キー無しの1文字で発動するものです。サムネイル一覧にフォーカスがある時のみ有効です。素の `C`/`V` と `Ctrl+C`/`Ctrl+V` は別の動作なので混同しないようご注意ください。

| キー | 動作 |
|---|---|
| `Ctrl+C` | 選択ファイルをクリップボードにコピー登録します。エクスプローラー等、D-liner以外のアプリへの貼り付けにも使えます。 |
| `Ctrl+X` | 選択ファイルをクリップボードに切り取り登録します（貼り付け時に移動として処理されます）。 |
| `Ctrl+V` | クリップボードのファイルを**現在表示中のフォルダ**へ貼り付けます。エクスプローラー等、D-liner以外のアプリでコピー/切り取りしたファイルも受け付けます。 |
| `F2` | 選択中の画像を1件リネームします（複数選択時は無効です。連番などの一括リネームは「ファイル」メニューの専用機能をご利用ください）。 |
| `F5` | 更新（フォルダ差分の再スキャン＋解像度等メタデータの補完） |
| `F8` | 現在表示中のフォルダの直下に新規フォルダを作成します。 |
| `Ctrl+F` | タグ検索欄にフォーカスを移し、既存の入力内容を全選択します。 |
| `Ctrl+A`（サムネイル一覧） | 現在の絞り込み結果（表示中の画像）を全選択します。一括タグ操作（6-4節）などにご利用ください。 |
| `Escape`（検索欄にフォーカスがある時） | 検索テキストをクリアし、サムネイル一覧にフォーカスを戻します。 |
| `C` | （ベアキー）選択ファイルを宛先フォルダ選択ダイアログ経由でコピーします。 |
| `M` | （ベアキー）選択ファイルを宛先フォルダ選択ダイアログ経由で移動します。 |
| `V` | （ベアキー）選択画像をSDIビューアで開きます。 |
| `Enter` / `Return`（サムネイル一覧） | （ベアキー）選択画像をSDIビューアで開きます（`V`と同じ動作）。 |
| `T`（サムネイル一覧） | （ベアキー）選択中の画像へタグを一括追加/削除するダイアログを開きます（6-4節）。 |
| `T`（SDIウィンドウ） | （ベアキー）表示中の画像へ手動タグを追加するダイアログを開きます（6-2節）。**サムネイル一覧の`T`とは別の動作です。** |
| `Delete` | 選択ファイル（フォルダを選択している場合はそのフォルダごと）を削除します。確認ダイアログが表示され、可能な環境ではゴミ箱に移動します（下記コラム参照）。 |
| `+` / `-` | サムネイルサイズを拡大/縮小します。 |
| `S`（SDIウィンドウ） | （ベアキー）タグパネルの検索/コピーモードを切り替えます（6-5節）。v0.8でマニュアルへの記載漏れを修正（機能自体はv0.7時点で実装済み）。 |

> **削除はゴミ箱経由です（可能な環境では）**
> ファイルやフォルダの削除は、`send2trash` パッケージが利用可能な場合はゴミ箱への移動として行われ、誤って削除してしまっても復元できます。`send2trash` が導入されていない環境（多くの場合、本機能追加前にセットアップ済みの環境）では、削除前に「ゴミ箱機能が利用できません」という警告が表示され、明示的に同意した場合のみ元に戻せない完全削除が行われます。警告が出た場合は `setup_runtime_env.bat` を再実行することでゴミ箱経由の削除が有効になります。

### 6-8. 個別設定でのタグ付けやり直し

自動タグ付けの結果に満足できない場合に、その画像またはフォルダだけ一時的に閾値・モデルを変えてタグ付けをやり直せる機能です。

- サムネイル一覧で画像を右クリック→「選択画像を別設定でタグ付けし直す(R)...」、または「タグ」メニュー→「選択中の画像を別設定でタグ付けし直す(Y)...」から開きます。
- 対象は、選択中の画像（複数選択可）、または未選択時はDB登録済みフォルダ全体です。**未登録フォルダの画像は対象になりません。**
- ダイアログで指定したモデル・閾値は**その回限り**で使われ、通常設定（タグ付け設定ダイアログの内容）には保存されません。
- 一般タグ閾値を0.15以下に設定した場合、5章と同様の警告が表示されます。

### 6-9. LoRA用へのエクスポート

選択した画像（未選択なら現在の絞り込み結果全体）を、指定した新規フォルダへ「画像のコピー＋同名.txtファイルへのタグ書き出し」としてエクスポートする機能です。元の画像・DB上のタグは変更されません。

- サムネイル一覧で画像を右クリック→「LoRA用にエクスポート(E)...」、または「タグ」メニュー→「LoRA用にエクスポート(E)...」から開きます。
- 出力先は、ファイルのコピー/移動時に使う既存フォルダ選択画面とは別に、「親フォルダ＋新規フォルダ名」を指定する専用画面です。クイックアクセス登録フォルダから親フォルダを素早く選べます。
- キャプション種別は「AIタグ＋マニュアルタグ」「マニュアルタグのみ」の2択です（いずれも`rating`/`meta`カテゴリのタグは除外されます）。
- 出力先フォルダは意図的に監視対象（DB登録フォルダ）として登録されません。これにより、エクスポートした画像がバックグラウンド自動タグ付けの対象になる心配がありません。
- 同名ファイルが複数の元フォルダから集まる場合は、連番を付けて自動的に衝突を回避します。

### 6-10. その他の細かな改善（v0.7以降）

- **ファイル名の自然順ソート**: 「ファイル名」でのソート時、`ファイル_2.png`のような連番のあるファイル名が桁数の違いに関わらず直感的な順序（2 → 3 → …→ 21）で並ぶようになりました。DB登録済み・未登録フォルダのどちらでも有効です。
- **SDIウィンドウのサイズ記憶**: 表示モード設定が「ウインドウサイズへ伸縮」「縦横固定して伸縮」「幅に合わせる」のいずれかの場合、最後に閉じたときのウィンドウサイズを次回開いたときに復元します（「そのまま」「大きい画像のみ縮小」は画像サイズへ自動追従するため対象外です）。

---

## 7. バックグラウンド処理について（ご利用前に必ずお読みください）


D-liner は画面を触っていない間も、以下の優先順位でバックグラウンド処理を行う設計になっています。

1. 画面表示（選択中フォルダのサムネイル・プレビュー表示）— 最優先
2. 選択中フォルダの未タグ付け画像のタグ付け
3. サムネイルの先読み（スクロールに備えた事前生成）
4. アイドル時に限り、選択中フォルダ以外の未タグ付け画像のタグ付けを行う

> **重要: アプリを開いたまま放置すると、④により他フォルダのタグ付けが自動的に進みます**
> - 画面上は何も操作していないように見えても、④の処理によって CPU / GPU / NPU の使用率が上がったり、ディスクアクセスが発生したりすることがあります。ノートPCではファンの動作音・発熱が増える場合があります。
> - ①〜③の操作（表示・現在フォルダのタグ付け・先読み）は④より優先されるため、通常の操作感が④によって損なわれることは基本的にありません。
> - ④の処理自体を止めたい場合は、対象フォルダのタグ付けが完了している状態にする、またはアプリを終了してください（バックグラウンド処理のみを個別にON/OFFする設定は現時点ではありません）。

---

## 8. メモリ使用に関する注意事項

プレリリース版としてご利用いただく上で、以下のメモリ使用に関する仕様をあらかじめご了承ください。

- 同時にロードされるタグ付けモデルは常に1つに制限されています（モデルを切り替えると、旧モデルの解放後に新モデルがロードされます）。ただしモデル切り替えの瞬間はメモリ使用量が一時的に変動します。
- D-liner はユーザー自身のローカル画像を対象とするため、Pillow が標準で行う「極端に高解像度な画像の読み込み制限（デコンプレッションボム対策）」を意図的に無効化しています。これにより、非常に高解像度なスキャン画像やイラストも正しくタグ付けできますが、裏を返すと極端に大きい画像を大量に処理する場合、通常より多くのメモリを消費する可能性があります。
- 数千〜数万枚規模の大きなフォルダを開く場合、サムネイルキャッシュ（画像管理DBおよびメモリ上のキャッシュ）の分だけメモリ・ディスク使用量が増加します。
- 上記はいずれも「動作に支障をきたすほどの異常な消費」を意図した仕様ではありませんが、非力なマシン（メモリ4GB以下等）や、特殊なデータ（数億画素クラスの巨大画像を大量に含むフォルダ）を扱う場合は、動作の重さやメモリ使用量の増加として体感される可能性がある点をあらかじめご承知おきください。

---

## 9. 既知の制約事項

### 9-1. NPU と NVIDIA/AMD GPU は同一環境で併用できません

onnxruntime-openvino（NPU/Intel GPU 向け）と onnxruntime-directml（NVIDIA/AMD/Intel GPU 向け）は、どちらも同じ `onnxruntime` という名前でPythonにインポートされる別々の配布物であり、1つの実行環境（venv）には実質どちらか一方しかインストールできません。

そのため、たとえば「Intel NPU 搭載PCに、NVIDIA GPU を後から増設して両方使う」といった構成では、NPU 用の環境のままでは NVIDIA GPU 側のGPU高速化が効かず、CPU 動作にフォールバックします（Intel 製ではない GPU を選んだ場合、誤ってIntel iGPUに切り替わることのないよう、D-liner 側でも安全側の判定を行っています）。この場合、GPU 側を優先するなら `--runtime directml` で環境を作り直す必要があります（NPU 高速化とは同時に使えません）。

### 9-2. モデル自動ダウンロード失敗時の挙動

自動ダウンロードに一度失敗したモデルは、同一の起動セッション中は再試行されません（ネットワーク不通時に何度も同じ失敗を繰り返して処理が止まって見えることを防ぐための仕様です）。再試行するにはアプリを再起動してください。

### 9-3. タグ付け中にフォルダの登録を解除しても、進行中のタグ付けは止まりません

タグ付け開始直後にフォルダの監視登録を解除しても、バックグラウンドのタガー処理はキャンセルされず、開始済みの分はそのまま完了まで実行されます。次回アップデートで対応予定です。

### 9-4. クイックアクセス登録フォルダの画像は、詳細ペインにファイルサイズ・解像度が表示されないことがあります

フォルダツリーの「クイックアクセス」経由で開いたフォルダの画像について、詳細ペイン（ファイル名・サイズ・解像度の一覧）にサイズ・解像度が反映されない場合があります。次回アップデートで対応予定です。

### 9-5. 手動で削除したAI由来タグは、再タグ付けを行うと復活することがあります

タグの「×」でAI由来タグを削除しても、その削除履歴はDBに記録されません。そのため、後日その画像に対して（バックグラウンド自動タグ付け・明示的な「選択画像を再タグ付け」のいずれでも）再度AI推論が実行されると、AIは削除されたことを知らないため、同じタグを再び付け直してしまうことがあります。この動作を避けたい画像には、6-3節のロック機能を使い、その画像を今後のAI再タグ付け対象から明示的に除外することをお勧めします。

---

## 10. トラブルシューティング

| 症状 | 対処 |
|---|---|
| 起動直後に何も表示されず終了する / 落ちる | `launch_d_liner_debug.bat`（コンソール表示あり）で同じ操作を再現し、表示されるログ・エラー内容をご確認ください。 |
| 「onnxruntime のインポートに失敗しました」と表示される | Visual C++ 2015-2022 Redistributable (x64) が未導入の可能性が高いです。https://aka.ms/vs/17/release/vc_redist.x64.exe から導入後、`setup_runtime_env.bat` を再実行してください。 |
| モデルの自動ダウンロードが終わらない／固まったように見える | ネットワーク環境（プロキシ・ファイアウォール）をご確認ください。ダウンロードは一定時間で自動的にタイムアウトし、アプリ本体が固まったままになることはありません。ブロックされた環境では4-3節の手動配置をご利用ください。 |
| 設定したはずのGPU/NPUが使われず、CPUで動作している | 9章の制約事項（NPUとNVIDIA/AMD GPUの同居不可）に該当していないかご確認ください。また、タグ付け設定でデバイスが正しく選択されているかもあわせてご確認ください。 |
| 新しく追加した画像の「解像度」列がいつまでも「-」のまま表示される | **仕様です。** 起動時の自動フォルダ同期は新規ファイルの検出のみを行い、解像度（幅×高さ）の取得はこの時点では行われません。解像度を反映させるには、画面上部の「更新 (F5)」ボタンを一度押してください（差分検出後にメタデータ補完が実行され、その画像の解像度が反映されます）。なお、フォルダ一覧に表示される行のうちサブフォルダそのものを表す行は元々ファイルではないため、この場合は「-」表示のままで正常です。 |
| 「予期しないエラーが発生しました」というダイアログが表示された | アプリ本体が異常終了する代わりに、想定外のエラーを検知してこのダイアログを表示する仕組みです（アプリは基本的にそのまま操作を続けられます）。同じ操作で繰り返し発生する場合は、D-linerと同じフォルダに生成される `crash_log.txt` の内容とあわせてご連絡いただけると原因調査がスムーズです。 |

---

## 11. お問い合わせについて

プレリリース版のため、上記以外にも想定外の不具合が見つかる可能性があります。再現手順（デバッグ起動時のログを含む）とあわせてご連絡いただけますと調査がスムーズです。

---

## 12. 変更履歴

2026-07-25（v0.8）

検索/コピーモード切替の`S`キーショートカットを追加。（6-5節・6-7節）。※機能自体はv0.7時点で実装済みでしたが、本書に「現時点では未対応」という誤った記載が残っていたため今回修正しました。
個別設定でのタグ付けやり直し機能を追加。（新設6-8節）
選択した画像（未選択なら現在の絞り込み結果全体）を、指定した新規フォルダへ「画像のコピー＋同名.txtファイルへのタグ書き出し」としてエクスポートする機能を追加。（新設6-9節）
SDIウィンドウのサイズを記憶するように変更。（新設6-10節）
一般タグ閾値が低い場合の警告を追加。（5章に追記）
ファイル名をより自然順なソートになるように変更（新設6-10節）
コピーモード「終了時の挙動」設定（維持する）でSDIウィンドウを閉じ、別画像を新規に開いた際、モード自体は維持されるが背景色だけ検索モードの色に戻ってしまう不具合を修正（v0.7時点からの既存バグ）。表示直後にスタイルの再計算を強制する対応で解消。
一般タグ閾値の低下警告が、スピナー操作で閾値を下げた場合に正しく表示されないことがある不具合を修正。閾値0.15への到達をスピナー（0.05刻み）で行うと、表示上は「0.15」でも内部的には浮動小数点演算の誤差により表示されない値（0.15000000000000002）になり、警告表示の判定に漏れが生じていました。キーボードで直接「0.15」と入力した場合は再現しないため見過ごされていました。
「選択中の画像を別設定でタグ付けし直す」ダイアログに、上記の低閾値警告が実装されていなかった点も修正し、両方のダイアログで共通の判定処理を使うよう統一。
内部改善（画面の見え方に影響しない変更）: 監視フォルダのクイックアクセス設定を管理するデータベース列の初期化処理を1箇所に整理。フォルダパスの表記ゆれの扱い方を整理し、登録処理と検索処理で判定方法を統一。

2026-07-19（v0.7）
タグパネルに「コピーモード」を追加。メニューバー右端の🔍/📋アイコンで検索モードと切り替えられます。コピーモード中はタグクリックで複数選択でき、選択したタグを「コピー」（クリップボードへ、rating/metaカテゴリは除外）、または「この組み合わせで検索」（検索欄を全置換してAND検索）できます。誤操作防止のため、コピーモード中はタグの×削除ボタンが無効化されます。詳細は6-5節を参照。
メニューバー右端に🏷「全タグをコピー」ボタンを追加。モードに関わらず、折りたたまれている分も含め表示中の全タグをコピーできます。
「オプション」メニューに「コピーモード終了時の挙動」を追加。SDIウィンドウを閉じたときにコピーモードを維持するか、検索モードへ自動的に戻すか選べます（既定は「閉じたら検索モードに戻す」）。
サムネイル一覧の右クリックメニューに「似たタグの画像を探す」を追加。キャラクタータグ・一般タグを自動抽出して類似画像を検索します（`1girl`等の汎用タグは自動除外）。詳細は6-6節を参照。
複数選択中に右クリックした場合、「名前の変更...」が実行できてしまい、右クリックした1件だけが密かにリネームされる不具合を修正（`F2`キーと同じく、複数選択時は無効化されるようにしました）。
ウィンドウ幅を狭めた際、メニューバー右端のアイコンが通常のメニュー項目（ファイル/画像等）を圧迫し、オーバーフロー表示（`>>`）が起きる前に項目自体が消えてしまう不具合を修正。

2026-07-18
手動タグ機能を追加(タグパネルの右クリックメニュー「タグを追加...」「既存タグ一覧から選択...」、`T`キーショートカット、オートコンプリート付き)。手動追加したタグは、タグキャッシュのクリアや再タグ付けを行っても削除されません。詳細は6-2節を参照。
タグの削除方法を変更。タグ右側の「×」クリックで削除する方式になりました(右クリックはSDIウィンドウのページ送りと衝突するため廃止)。手動タグの削除時のみ確認ダイアログが表示されます。詳細は6-1節を参照。
AI自動タグ付けのロック機能を追加。画像右上の🔒/🔓アイコンで、特定の画像をバックグラウンド自動タグ付けの対象から明示的に除外できるようになりました。詳細は6-3節を参照。
複数画像への一括タグ追加/削除機能を追加(「タグ」メニュー、サムネイル一覧の右クリックメニュー、`T`キー)。LoRA学習用のトリガーワードなどをまとめて仕込む用途を想定しています。詳細は6-4節を参照。
サムネイル一覧に`Ctrl+A`(全選択)・`Enter`/`Return`(SDIビューアで開く)のショートカットを追加。
検出タグ数が多い画像で、3行を超える分が表示されず存在ごと消えていた不具合を修正。「+N件」表示から全展開できるようになりました。
複数画像への一括タグ操作を実行した際にアプリ本体が異常終了することがあった不具合を修正。あわせて、想定外のエラー発生時にアプリ全体を巻き込まずエラーダイアログ表示に留める仕組み(`crash_log.txt`への記録を含む)を追加。

2026-07-12
キーボードショートカットを追加（Ctrl+C/Ctrl+X/Ctrl+Vによるファイルのコピー・切り取り・貼り付け〈エクスプローラー等との相互利用可〉、F2リネーム、F8新規フォルダ作成、Ctrl+F検索欄フォーカス、Escape検索クリア）。詳細は6-7節を参照。
フォルダ削除時に発生していた [WinError 5] アクセスが拒否されました エラーを修正。
ファイル/フォルダ削除をゴミ箱経由（send2trash）に変更し、誤削除時に復元できるようにした。(setup_runtime_envによるvenvの再構築が必要)
ゴミ箱への移動時にパス区切り文字の不整合により発生していた [Errno 2] エラーを修正（フォルダ削除・通常ファイル削除・SDIビューアからの削除いずれも）。
Deleteキーによる削除が反応しない不具合、および削除確認ダイアログで矢印キー移動後にEnterキーが正しく機能しない不具合を修正。
削除確認ダイアログのデザイン・既定ボタンをサムネイル一覧・SDIビューアで統一。
F8での新規フォルダ作成時に表示中フォルダが意図せず切り替わっていた不具合を修正（ハイライトのみ行い、新規フォルダはサムネイル一覧にも自動反映されるようにした）。
複数選択したファイルをドラッグ&ドロップした際に1件しかコピー/移動されない不具合を修正。
タグ／ファイル名検索の絞り込み待機時間を300msから1000msに延長し、入力途中で意図せずフォーカスが外れる問題を軽減。

2026-07-11 プレリリース版 (v0.6)
モデル自動ダウンロード時のハング・クラッシュ問題を解決（ダウンロード処理の子プロセス分離）。
検証機でのDirectML誤検出バグ（大文字小文字判定ミス）を修正。
「モデルフォルダ(手動指定)」機能を新設。
NPU非搭載機のデバイス選択肢からNPUを除外。